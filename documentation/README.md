# Documentation index

Package-level narrative documentation for `wlist`, following the `trends`
documentation-architecture template (ecosystem D-025/D-027).

This folder is a scaffold — created 2026-07-12 (ecosystem D-038) alongside
`range`/`sites` equivalents, ahead of content. Populate as `wlist`'s own
usage guide / workflow reference / deep dives are written; see
`../../trends/documentation/README.md` for the shape to follow.
